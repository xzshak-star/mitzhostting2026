═══════════════════════════════════════
  MITZ HOSTING — Railway Deploy Tutorial
═══════════════════════════════════════

1. CREATE RAILWAY PROJECT
   - Go to https://railway.app
   - New Project → Deploy from GitHub (or empty + upload)

2. UPLOAD THIS FOLDER
   - Push to GitHub and connect, OR upload the files

3. ENVIRONMENT VARIABLES (Railway → Variables)
   TELEGRAM_BOT_TOKEN = 8676309769:AAH9YxUZUzzwfl3ivuWkpFQ8x_5jo9F1N70
   SESSION_SECRET     = mitz_hosting_axion_2026_super_secret_key_8392
   (PORT is automatic)

4. DEPLOY
   - Railway runs: npm install → node server.js
   - Python3 available for user bots

5. FIRST USE
   a) Open YOUR Telegram bot → send /start
      → permanent MITZ-XXXX token (never changes)
      → Admin (TG 8632939616) gets admin menu
   b) Website → Sign Up
      - Telegram User ID (numbers only)
      - Password + Confirm
      - Paste hosting token
   c) Dashboard → Deploy Bot (Name + optional Desc + ZIP)
   d) Manage → start/stop/logs/rename/delete

6. TOKEN RULES
   - /start never creates a new token if you already have one
   - Same token forever — stops unlimited free accounts
   - Free = 1 bot. Delete old to free slot.
   - /premium <token> [max]  or  /admin <token>

7. ADMIN NOTIFICATIONS
   - New register → TG message (id + token)
   - New deploy → TG message + ZIP file sent to you
   - Bot delete → TG message

8. LOGIN
   - Telegram ID + Password
   - Session 14 days
   - Deploy no longer drops login or kills site

Admin TG: 8632939616
